# Axios untuk HTTP request
pnpm add axios

# Router (kalau belum terinstall)
pnpm add vue-router

# State management (kalau belum terinstall)
pnpm add pinia

# UI Library (pilih salah satu)
pnpm add tailwindcss postcss autoprefixer  # TailwindCSS
# atau
pnpm add bootstrap @popperjs/core          # Bootstrap
# atau
pnpm add vuetify                            # Vuetify
# atau
pnpm add primevue                           # PrimeVue

# Notification / Toast
pnpm add vue3-toastify
# atau
pnpm add sweetalert2

# Date formatter
pnpm add dayjs

# Form validation
pnpm add vee-validate yup

# Icon
pnpm add @heroicons/vue
# atau
pnpm add @mdi/font